# React 19 / React Native 0.82 peer ranges across the widget packages and the
# web test deps (react-native-web, testing-library) don't all overlap cleanly.
# Installing with legacy peer resolution keeps installs reproducible in CI.
legacy-peer-deps=true
